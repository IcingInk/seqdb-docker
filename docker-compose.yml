fs.tomcat:
  image: tianon/true
  volumes:
    - ./srv/logs:/opt/tomcat/logs

fs.db:
  image: tianon/true
  volumes:
    - ./mysql_datadir:/var/lib/mysql
    - ./mysql_autoload:/docker-entrypoint-initdb.d:ro

db:
  image: mysql:5.6
  ports:
    - "13306:3306"
  env_file: .env
  volumes_from:
    - fs.db

tomcat:
  image: dina/seqdb:v3.17
  ports:
    - "8080:8080"
  environment:
    - VIRTUAL_HOST=seqdb.nrm.se
    - VIRTUAL_PORT=8080
  links:
   - db:db
  volumes_from:
  - fs.tomcat
